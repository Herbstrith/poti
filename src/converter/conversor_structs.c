#include "conversor_struct.h"

/*
void add_event_to_list(header_event new_event, header_event_list_item *event_list)
{
	header_event_list_item *actual_event;
	while(actual_event != NULL)
	{
		if(actual_event->next == NULL)
		{
			break;
		}
		actual_event = event_list->next;		
		
	}

	actual_event->next = new_event;
	

}*/
